# Verification record

September 14, 2026 audit and upstream upgrade. This is a development record, not a release certification.

## Confirmed results

- The production Swift model regression executable passed all 44 assertions on hosted macOS in [run 34836151132](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34836151132). It covers thread isolation, stale responses, draft recovery, duplicate-send prevention, advertised approval decisions, disconnect state, isolated demo responses, the bundled schema reference, manifest-derived upstream source links, dynamic/hidden model pagination, backward history pagination and safe Files-picker cancellation. History pages deduplicate both existing items and duplicates within a returned page. A delayed first send now snapshots its original settings and preserves text typed during creation; failed sends preserve both rejected and newer draft text.
- That same run passes the production Foundation WebSocket client against a loopback fixture: initialization, Unicode/multiline JSON, server-request replies, structured errors, cancellation, disconnect, reconnect, a 35-second idle interactive request and server-close failure state. This is a real network-client test, but the server is a fixture, not Codex.
- The candidate protocol gate matches 166 client methods, 11 server requests and 84 notifications against upstream `d77ebc72237a639b6d877f2edc3b20b54631f25e`.
- The i686 compatibility patch applies to the candidate. The former V8 in-process stubs are no longer necessary.
- The candidate cross-compilation and Alpine packaging passed in [run 34824661997](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34824661997). Its original app artifact predates subsequent emulator/UI repairs and must not be treated as the final verified build.
- All three simulator profiles passed in [run 34832827978](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34832827978), again in [run 34835083172](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34835083172), and with the latest manifest-derived links in [run 34836936785](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34836936785). The 13-inch desktop test verifies actual application-level typing without another field tap after sending, Features dismissal and Terminal recovery. The 11-inch touch test covers Show all, a demo folder link, Feature Center navigation, the Start request form and Terminal recovery. The mini accessibility/Dark Mode test opens the compact Files workbench and returns from Terminal. Saved screenshots were inspected; these remain demo-transport tests.
- Earlier simulator failures exposed workbench geometry feedback and accessibility identifiers propagating into child controls. Both were repaired. Test-only failures from unreliable `hasFocus` assertions and querying offscreen lazy form rows were replaced with behavioral typing assertions and existence-aware scrolling; those failures were not waived.
- Real-emulator regression programs pass on both Linux and Darwin for MOVMSKPS, CVTDQ2PD, overlapping SIMD shuffles, masked futex wakeups, timeout validation, parent-death signals, clock sleeps, threaded parent process IDs and socket-pair flags/cleanup. [Run 34832260722](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34832260722) passes these checks but correctly fails its later command probe against the unadapted Rust binary.
- Both the old and candidate packaged runtimes complete initialization, `account/read`, `model/list` and `fs/readDirectory` inside the repaired Linux-hosted iSH emulator. The SQLite allocator assertion was caused by aliased SIMD operations overwriting source lanes; it was fixed without bypassing the allocator check.
- Darwin-hosted Codex initialization and account/model/filesystem reads also pass after repairing host socket-constant translation and a host `uname` buffer overflow. The latter has a dedicated hostname-bounds regression; no allocator or bounds assertion was disabled.
- The version-locked Rust pipe-spawn build succeeded in [run 34833456831](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34833456831), including an early real-iSH worker-thread spawn regression. Linux then passed complete stdio and WebSocket command probes in [run 34836617326](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34836617326). Darwin also passed both command transports in [run 34838685854](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34838685854), but that run failed automatic boot. These binary results are for `5b1d65601816`, not the newer final pin.
- Automatic boot exposed missing OpenRC packaging, unsupported tmpfs initialization, and a regular-file `poll()` readiness hang. Packaging now installs OpenRC and a narrow iSH sysinit adapter. Kernel fixes address regular-file readiness, invalid descriptors, duplicate poll entries and one-shot event lifetimes. Dedicated polling regressions and the full init-to-WebSocket gate are required before accepting these changes.
- Intermittent host SIGSEGV failures were captured at `mem_ptr`'s copy-on-write `memcpy`, both by the Linux core dump in [run 34839990134](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34839990134) and Darwin AddressSanitizer in [run 34839462623](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34839462623). The code retained backing-page pointers across a read-to-write lock gap. Fault resolution now re-reads mappings while holding the write lock, and a 256-round two-writer regression exercises this race. Futex loads also sample their word before releasing the mapping lock.
- The latest pinned `d77ebc72237a` runtime passes the combined Linux instruction, copy-on-write, stdio, WebSocket and automatic OpenRC boot gates in [run 34840433882](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34840433882). Each transport performs 20 additional real shell launches. Ordinary Darwin and AddressSanitizer gates are still pending; this is not yet a release verdict.
- The earlier rebuilt-standard-library artifact from run 34829847033 is rejected: `git apply` silently skipped the nested rust-src paths while returning success. The build now initializes an isolated patch root, checks the resulting source and reverse applicability, and executes a small patched-Rust worker-thread spawn test inside real iSH before compiling all of Codex. Recompilation alone is not treated as evidence that a patch took effect.

## Changes covered by the audit

- Thread-local active turns, plans, diffs and drafts; stale resume/directory response protection; paginated history access.
- Failed-send draft recovery, duplicate-start exclusion, late-completion races and correct Stop targeting.
- RPC cancellation, finite ordinary-request deadlines, connection teardown and truthful offline UI.
- Compact-window workbench and pending-request inbox; MCP form/URL acceptance; advertised command decisions.
- Deferred sheet transitions, account-row hit area, adaptive accessibility header, reduced-motion status animation and preserved multiline output.
- Demo preferences and transport no longer touch real saved workspace settings or fail a fake send against a disconnected real RPC client.
- Required-field scaffolding and version-pinned offline JSON parameter reference. Advanced request routing is not equivalent to dedicated forms or end-to-end feature validation.
- Guest socket-pair host-constant translation and descriptor ownership on `EFAULT`/`EMFILE`; a new assembly test covers nonblocking I/O, close-on-exec flags, EOF and cleanup.
- A guest-only Rust 1.95 standard-library patch selects the existing Unix error pipe rather than inventing incorrect packet-socket semantics. Unsupported pidfd requests on this path fail explicitly. Both compiler version and commit must match before applying the patch.
- Saved guest roots are not overwritten by app upgrades. A read-only runtime-manifest check blocks incompatible GUI/runtime versions before refreshing the workspace; missing or malformed metadata also fails closed. The 48-assertion native model suite, including matching/mismatched/missing runtime checks, passed in [job 103961758183](https://github.com/j0shua-SYSON/codex-for-ipad/actions/runs/34839767558/job/103961758183) before the superseded workflow's remaining jobs were canceled. Automatic in-place user-data migration is not implemented.

## Evidence boundaries

The iPad UI tests launch `--codexpad-demo`. They verify rendering and input behavior against simulated responses, not authentication, inference, a live Files provider, MCP servers, external integrations or real command execution. Tests now require an actual labelled demo response after sending and exercise the accessibility workbench. Saved screenshots and test bundles must be reviewed, including failures.

The headless iSH probe uses the actual emulator and packaged binary with no model credentials. Linux and Darwin host jobs require instruction regressions, initialization, account/model reads, filesystem reads and harmless Git/ripgrep execution, over stdio, WebSocket and packaged `/sbin/init` automatic startup. They also require correct nonzero command exits, stderr capture and missing-executable errors. Command probes have passed on both hosts, but the combined automatic-boot/stability gate has not. Even successful probes would not prove physical-iPad performance, foreground/background behavior, provider sign-in, sandbox isolation, hardware keyboard/pointer behavior or App Store distribution.

No physical 13-inch iPad Pro test has been performed during this audit. HIG compliance remains a checklist and verification target, not a certification. Do not advertise this as a finished full local port until the runtime and device checks pass.

## Upstream candidate

The candidate lives on `upgrade/codex-5b1d65601816` until compatibility work is verified. The old pin is `6bd3f5e3db8275c10c7e4bbcc1342c32a89b7eee`; upstream changes include projects, sections, attachments, prompt queues, user verification, Bedrock auth and paginated history. `thread/rollback` was removed in favor of `thread/revert`.

Upstream advanced from `5b1d65601816` to `d77ebc72237a` during verification. The single additional commit changes Code Mode timer cancellation in two files; it changes neither the app-server schema nor the Rust toolchain. The final pin follows that newer snapshot. Runtime build evidence for the earlier snapshot is labelled as such until the final pinned binary is rebuilt and verified.

Build/run outcomes are being collected. Failed gates are not waived or described as successful.
